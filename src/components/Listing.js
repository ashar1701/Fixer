import { 
    Card,
    CardContent,
    Typography
 } from '@mui/material';

const Listing = () => {
    return <div className='listing'>
        <Card>
            <CardContent>
                <Typography variant="h1">Listing</Typography>
            </CardContent>
        </Card>
    </div>
}

export default Listing;